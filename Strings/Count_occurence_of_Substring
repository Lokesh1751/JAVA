public class substroccur {
    // Write a Java program to count the occurrence of a substring in a given string
    // using StringBuilder.
    public static void main(String[] args) {
        StringBuilder str = new StringBuilder("sujalshshshsujalsijdidjiskjsujalsidjisujaljusdujsujal");
        String ans1 = "sujal";
        int cnt = 0;
        for (int i = 0; i <= str.length(); i++) {
            for (int j = i + 1; j <= str.length(); j++) {
                String ans = str.substring(i, j);
                if (ans.equals(ans1)==true) {
                    cnt++;
                }
            }
        }
System.out.println(cnt);
    }
}
